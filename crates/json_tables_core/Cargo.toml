[package]
name = "json_tables_core"
version = "0.3.0"
edition = "2021"
description = "Normalisation core for Exasol JSON Tables: JSON documents in, relational table-family rows out."
license = "MIT"

[dependencies]
serde_json = { version = "1.0.150", features = ["preserve_order"] }
